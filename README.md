# Scientific Research Assistant

一个基于 AI 多智能体系统的科研工程桌面应用，支持科研选题管理、智能论文生成、行业动态追踪和竞品分析。

## 🌟 核心功能

### 1. 科研选题管理
- 创建和管理研究选题
- 按领域、关键词搜索和分类
- AI 辅助选题推荐

### 2. 多智能体论文生成
采用 6 个专业 AI 智能体协同工作：
- **研究主管** (Research Director) - 总体协调和计划制定
- **文献调研员** (Literature Researcher) - 文献搜集和综述
- **方法论专家** (Methodology Expert) - 研究方法设计
- **数据分析师** (Data Analyst) - 数据分析方案
- **论文撰写员** (Paper Writer) - 论文撰写
- **同行评审员** (Peer Reviewer) - 质量评估和改进建议

### 3. 行业动态追踪
- 实时获取最新科研资讯
- AI 智能摘要和关键词提取
- 相关度评分和推荐

### 4. 竞品分析系统
- 自动搜索同领域论文
- AI 对比分析和差异化建议
- 引用数据和影响力评估

## 🏗️ 技术架构

### 前端
- **框架**: Electron + React + TypeScript
- **UI 库**: Ant Design
- **状态管理**: Redux Toolkit
- **构建工具**: Vite

### 后端
- **框架**: Python FastAPI
- **数据库**: SQLite + SQLAlchemy
- **AI 引擎**: Anthropic Claude API
- **异步支持**: asyncio + aiosqlite

### 多智能体系统
- 基于 Anthropic Claude 3.5 Sonnet
- 角色分工型架构
- 实时进度推送 (WebSocket)

## 📦 安装和运行

### 前置要求
- Node.js 18+
- Python 3.10+
- npm 或 yarn

### 1. 安装依赖

#### 前端依赖
```bash
npm install
```

#### 后端依赖
```bash
cd backend
pip install -r requirements.txt
```

### 2. 配置环境变量

创建 `backend/.env` 文件（可选，已在 config.py 中配置默认值）：
```env
ANTHROPIC_API_KEY=your-api-key-here
DATABASE_URL=sqlite+aiosqlite:///./database/scientific.db
```

### 3. 启动应用

#### 开发模式

**方式一：同时启动前端和后端（推荐）**
```bash
# 终端 1: 启动后端
npm run start:backend

# 终端 2: 启动前端和 Electron
npm start
```

**方式二：分别启动**
```bash
# 终端 1: 启动 React 开发服务器
npm run start:react

# 终端 2: 启动 Electron
npm run start:electron

# 终端 3: 启动 FastAPI 后端
npm run start:backend
```

#### 生产模式

构建桌面应用：
```bash
npm run build
npm run package
```

打包后的应用将生成在 `dist/` 目录中。

## 📁 项目结构

```
scientific-project/
├── electron/                    # Electron 主进程
│   ├── main.js                 # 主进程入口
│   ├── preload.js              # 预加载脚本
│   └── ipc-handlers.js         # IPC 处理器
│
├── src/                        # React 前端
│   ├── components/             # UI 组件
│   │   ├── TopicSearch/        # 选题搜索
│   │   ├── PaperGenerator/     # 论文生成
│   │   ├── IndustryMonitor/    # 行业动态
│   │   └── CompetitorAnalysis/ # 竞品分析
│   ├── store/                  # Redux 状态管理
│   ├── services/               # API 服务
│   └── App.tsx                 # 根组件
│
├── backend/                    # Python 后端
│   ├── main.py                 # FastAPI 入口
│   ├── config.py               # 配置管理
│   ├── agents/                 # 智能体系统
│   ├── services/               # 业务服务
│   │   └── orchestrator.py     # 多智能体协调器
│   ├── models/                 # 数据模型
│   ├── api/                    # API 路由
│   └── utils/                  # 工具模块
│       └── anthropic_client.py # Anthropic API 客户端
│
├── database/                   # SQLite 数据库
│   └── scientific.db           # 数据库文件（自动创建）
│
├── package.json                # Node.js 配置
├── requirements.txt            # Python 依赖
└── README.md                   # 项目文档
```

## 🚀 使用指南

### 1. 创建研究选题
1. 点击"选题搜索"标签
2. 点击"新建选题"按钮
3. 填写选题标题、研究领域、关键词和描述
4. 点击"创建"

### 2. 生成论文
1. 切换到"论文生成"标签
2. 从下拉菜单选择一个研究选题
3. 点击"开始生成"按钮
4. 查看实时进度：系统将显示 6 个智能体的工作流程
5. 生成完成后，查看论文内容和质量评分
6. 点击"导出论文"保存为 Markdown 文件

### 3. 查看行业动态
1. 切换到"行业动态"标签
2. 点击"刷新动态"获取最新资讯
3. 浏览相关领域的最新研究进展
4. 点击"查看原文"访问完整文章

### 4. 竞品分析
1. 切换到"竞品分析"标签
2. 选择一个研究选题
3. 点击"搜索竞品"
4. 查看同领域的相关论文
5. 点击"AI 分析"获取详细对比分析

## 🔧 API 端点

### Topics (选题)
- `GET /topics` - 获取所有选题
- `POST /topics` - 创建新选题
- `GET /topics/{id}` - 获取单个选题
- `GET /topics/search?q=query` - 搜索选题
- `DELETE /topics/{id}` - 删除选题

### Papers (论文)
- `GET /papers` - 获取所有论文
- `POST /papers/generate` - 生成论文
- `GET /papers/{id}` - 获取单个论文
- `WS /ws/paper/{id}` - WebSocket 实时进度
- `DELETE /papers/{id}` - 删除论文

### Industry (行业动态)
- `GET /industry/news` - 获取行业资讯
- `POST /industry/refresh` - 刷新资讯
- `GET /industry/news/{id}` - 获取单条资讯

### Competitors (竞品)
- `GET /competitors?topic_id={id}` - 获取竞品列表
- `POST /competitors/{id}/analyze` - AI 分析竞品
- `GET /competitors/{id}` - 获取单个竞品

## 🤖 智能体工作流程

```
用户选择选题
    ↓
研究主管分析选题，制定计划
    ↓
文献调研员搜集和整理文献
    ↓
方法论专家设计研究方法
    ↓
数据分析师提供分析方案
    ↓
论文撰写员撰写完整论文
    ↓
同行评审员评估质量并评分
    ↓
生成最终论文
```

## 📊 数据库架构

### topics (选题表)
- id, title, description, field, keywords, status, created_at, updated_at

### papers (论文表)
- id, topic_id, title, abstract, content, version, status, quality_score, created_at

### agent_conversations (智能体对话)
- id, paper_id, agent_role, message, iteration, created_at

### industry_news (行业资讯)
- id, title, source, url, content, keywords, relevance_score, published_at, created_at

### competitors (竞品论文)
- id, topic_id, title, authors, source, url, abstract, citations, published_at, analysis, created_at

## 🔐 安全说明

- API Key 已配置在 `backend/config.py` 中
- 生产环境建议使用环境变量管理敏感信息
- 数据库文件存储在本地，无需外部服务器

## 🛠️ 开发说明

### 添加新的智能体角色
1. 在 `backend/utils/anthropic_client.py` 中添加新的 system prompt
2. 在 `backend/services/orchestrator.py` 中更新工作流程
3. 在前端 `PaperGenerator.tsx` 中更新 UI 显示

### 扩展 API 功能
1. 在 `backend/models/` 中添加新的数据模型
2. 在 `backend/api/` 中创建新的路由文件
3. 在 `backend/main.py` 中注册新路由

## 📝 TODO

- [ ] 实现真实的学术数据库爬虫 (arXiv, PubMed)
- [ ] 添加论文导出为 PDF/Word 格式
- [ ] 实现多轮迭代优化机制
- [ ] 添加用户配置和偏好设置
- [ ] 实现云端数据同步
- [ ] 添加多语言支持

## 📄 许可证

MIT License

## 👥 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题或建议，请提交 GitHub Issue。

---

**Powered by Anthropic Claude 3.5 Sonnet** 🚀
